# Copy to .env.local and fill in. Falls back to the dev bypass (dev-project /
# dev-api-key) if unset. Note: CRA requires the REACT_APP_ prefix.
REACT_APP_RABIT_PROJECT_ID=
REACT_APP_RABIT_API_KEY=
REACT_APP_RABIT_API_BASE_URL=http://localhost:3001
