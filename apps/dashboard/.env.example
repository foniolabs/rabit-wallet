# Base URL of the @rabit/api server.
RABIT_API_URL=http://localhost:3001

# Project API key used by the dashboard to call /auth/* endpoints.
# Locally, the API auto-creates a "dev-project" for this key.
RABIT_API_KEY=dev-api-key
