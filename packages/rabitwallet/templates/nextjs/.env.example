# Copy to .env.local. Falls back to the dev bypass (dev-project / dev-api-key) if unset.
NEXT_PUBLIC_RABIT_PROJECT_ID=
NEXT_PUBLIC_RABIT_API_KEY=
NEXT_PUBLIC_RABIT_API_BASE_URL=http://localhost:3001

# Optional — Google sign-in + ERC-4337 smart accounts.
NEXT_PUBLIC_BUNDLER_URL=
NEXT_PUBLIC_PAYMASTER_URL=
