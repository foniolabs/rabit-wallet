packages:
  - "apps/*"
  - "packages/*"
  - "examples/*"
  - "tools/*"
  - "my-test-app"
  # Legacy pre-pivot packages (wallet-connector design).
  # Kept on disk for reference; excluded from the workspace.
  - "!packages/connectors"
  - "!packages/smart-accounts"
  # Expo app — standalone (heavy RN deps); run from its own dir. See its README.
  - "!examples/expo"
